import { Meta, Story, Preview, ArgsTable } from "@storybook/addon-docs";
import { Header } from "./";
import mocks from "./mocks.json";

<Meta
  title="Globals/Header"
  parameters={{
    actions: { handles: ["click a"] },
    chromatic: { viewports: [320, 1440] },
    nextRouter: {
      path: "/",
      asPath: "/",
    },
  }}
/>

# Header

<Preview withToolbar>
  <Story name="Header" args={mocks}>
    {(args) => <Header {...args} />}
  </Story>
  <div />
</Preview>

<ArgsTable story="Header" />
