import { Meta, Story, Preview, ArgsTable } from "@storybook/addon-docs";
import { DefaultContact } from "./";
import mocks from "./mocks.json";

<Meta
  title="Sections/Contact"
  component={DefaultContact}
  parameters={{
    actions: { handles: ["click a"] },
    chromatic: { viewports: [320, 1440] },
  }}
/>

# Contact

A simple contact form section to allow users to get in touch

<Preview withToolbar>
  <Story name="Contact" args={mocks.default}>
    {(args) => <DefaultContact {...args} />}
  </Story>
  <div />
</Preview>

<ArgsTable story="Contact" />
