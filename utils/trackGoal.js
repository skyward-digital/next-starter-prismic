// import { usePlausible } from "next-plausible";

/**
 * A generic tracking function that can used with any analytics provider, plausible is just an example here
 */
// export const TrackGoal = ({ goal }) => {
//   const plausible = usePlausible();

//   plausible(goal);

//   return null;
// };
